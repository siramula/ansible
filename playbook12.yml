---
- name: Implementing loops
  hosts: all
  tasks:
    - name: Install,uninstall,upgradation s/w's
      apt:
        name: "{{item.a}}"
        state: "{{item.b}}"
        update-cache: "{{item.c}}"
      with_items:
        - {a: tree,b: present,c: no}
        - {a: git,b: absent,c: no}
        - {a: maven,b: latest,c: yes}
...

